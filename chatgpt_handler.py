import json
import openai

from faiss_db.search import search_problem  # Импорт функции поиска из search.py
from config import OPENAI_API_KEY


# Настройка ключа API OpenAI
openai.api_key = OPENAI_API_KEY


def generate_final_response_dual(results_db1, results_db2, user_query):
    """Сформировать ответ на основе двух топ-списков (БД1 и БД2) с использованием вашего промпта.

    В пункте «Ответ основан» сначала выведи две секции: «База данных 1» и «База данных 2»,
    где перечисли номера идей, использованных из каждой; если в одной из баз нет релевантной
    информации — явно укажи это (например: «База данных 2: нет релевантных идей»).
    """
    print("[dual] Формирую ответ на основе двух баз...")
    search_result_pretty_db1 = json.dumps(results_db1, ensure_ascii=False, indent=4)
    search_result_pretty_db2 = json.dumps(results_db2, ensure_ascii=False, indent=4)

    messages = [
        {
            "role": "system",
            "content": (
                "Ты технический помощник в металлургической компании. "
                "Твоя задача — анализировать данные в формате JSON и выдавать структурированные, точные и полезные ответы по техническим вопросам, связанным с производством. "
                "Если информация есть в базе, используй её. Если информации нет, дай общие рекомендации, но обязательно уведоми пользователя об отсутствии информации. "
                "В пункте ‘Ответ основан’ сначала выведи две секции: ‘База данных 1’ и ‘База данных 2’, где перечисли номера идей, использованных из каждой; если в одной из баз нет релевантной информации — явно укажи это (например: ‘База данных 2: нет релевантных идей’)."
            ),
        },
        {
            "role": "user",
            "content": (
                f"Вопрос пользователя: {user_query}\n"
                f"Данные из векторной базы данных 'Фабрика идей'(возможные решения) — База данных 'Фабрика идей':\n```json\n{search_result_pretty_db1}\n```\n"
                f"Данные из векторной базы данных 'ЗТЗ' (возможные решения) — База данных 'ЗТЗ':\n```json\n{search_result_pretty_db2}\n```\n\n"
                "Данные приведены из двух БД ('Фабрика идей' и 'ЗТЗ') — используй обе. Ответ обязательно должен опираться на топ из 'Фабрика идей' и топ из 'ЗТЗ'.\n\n"
                "⚠ ВАЖНО! Перед тем как отвечать:\n"
                "- Если запрос содержит случайный набор букв (например, 'абоба', 'ывапавп'), сообщи пользователю: 'Ваш запрос не является осмысленным. Пожалуйста, уточните его.'\n"
                "- Если запрос связан с производством, оборудованием, технологическими процессами или безопасностью, ты должен дать ответ.\n"
                "- Если в базе есть релевантные данные, используй их для ответа.\n"
                "- Если в базе есть прямые или частично релевантные данные, используй их для ответа.\n"
                "- Если точных данных нет, используй идеи, которые могут быть полезны по смыслу.\n"
                "- Проанализируй, есть ли среди переданных данных релевантные решения. Если нет, сообщи пользователю: 'По вашему запросу нет информации в базе данных.'\n\n"
                "📌 Формат ответа:\n"
                "1️⃣ Есть ли информация по запросу? (Ответь ДА или НЕТ. Если НЕТ, сразу сообщи об этом пользователю и прекрати ответ.) Формат ответа: ДА | НЕТ\n\n"
                "2️⃣ Рекомендации по вопросу:\n"
                "- Выведи список рекомендаций, добавляя к каждой рекомендации номер идеи и статус, на основе которых она сделана.\n"
                "- Если в базе нет точных данных, сформулируй общие рекомендации на основе темы вопроса.\n"
                "- Обоснуй рекомендации: почему они подходят?\n"
                "- Пиши рекомендации развернуто и при необходимости дополняй деталями.\n"
                "Формат ответа:\n"
                "- развернутая рекомендация по проблеме (идея: 'номер идеи', 'статус')\n\n"
                "3️⃣ Ответ основан на:\n"
                "- Сначала выведи секцию 'Фабрика идей' со списком номеров идей из БД 'Фабрика идей'.\n"
                "- Затем выведи секцию 'ЗТЗ' со списком номеров идей из БД 'ЗТЗ'.\n"
                "- Если в одной из баз нет релевантных идей — явно укажи в какой базе идей нет а в из какой взяты идеи.(например: База данных 'Фабрика идей': нет идей\n\nБаза данных 'ЗТЗ':\n(список) \n"
                "Формат ответа:\n"
                "База данных 'Фабрика идей':\n(список) - номер идеи(чаще всего выглядит наподобие _2020_011_6131_A_УС_ЭСПЦ_)\n\nБаза данных 'ЗТЗ':\n(список) - номер идеи(чаще всего выглядит наподобие ЗИ №455 или №200)"
            ),
        },
    ]

    resp = openai.ChatCompletion.create(
        model="gpt5",
        messages=messages
    )

    text = resp["choices"][0]["message"]["content"].strip()
    tokens = resp.get("usage", {}).get("total_tokens", 0)
    return text, tokens


def generate_final_response(search_result_list, user_query):
    """Сформировать ответ на основе одного топ-списка (одна БД) с использованием вашего промпта."""
    print("[single] Формирую ответ на основе одной базы...")
    search_result_pretty = json.dumps(search_result_list, ensure_ascii=False, indent=4)

    messages = [
        {
            "role": "system",
            "content": (
                "Ты технический помощник в металлургической компании. "
                "Твоя задача — анализировать данные в формате JSON и выдавать структурированные, точные и полезные ответы по техническим вопросам, связанным с производством. "
                "Если информация есть в базе, используй её. Если информации нет, дай общие рекомендации, но обязательно уведоми пользователя об отсутствии информации."
            ),
        },
        {
            "role": "user",
            "content": (
                f"Вопрос пользователя: {user_query}\n"
                f"Данные из векторной базы данных (возможные решения):\n```json\n{search_result_pretty}\n```\n\n"
                "⚠ ВАЖНО! Перед тем как отвечать:\n"
                "- Если запрос содержит случайный набор букв (например, 'абоба', 'ывапавп'), сообщи пользователю: 'Ваш запрос не является осмысленным. Пожалуйста, уточните его.'\n"
                "- Если запрос связан с производством, оборудованием, технологическими процессами или безопасностью, ты должен дать ответ.\n"
                "- Если в базе есть релевантные данные, используй их для ответа.\n"
                "- Если в базе есть прямые или частично релевантные данные, используй их для ответа.\n"
                "- Если точных данных нет, используй идеи, которые могут быть полезны по смыслу.\n"
                "- Проанализируй, есть ли среди переданных данных релевантные решения. Если нет, сообщи пользователю: 'По вашему запросу нет информации в базе данных.'\n\n"
                "📌 Формат ответа:\n"
                "1️⃣ Есть ли информация по запросу? (Ответь ДА или НЕТ. Если НЕТ, сразу сообщи об этом пользователю и прекрати ответ.) Формат ответа: ДА | НЕТ\n\n"
                "2️⃣ Рекомендации по вопросу:\n"
                "- Выведи список рекомендаций, добавляя к каждой рекомендации номер идеи и статус, на основе которых она сделана.\n"
                "- Если в базе нет точных данных, сформулируй общие рекомендации на основе темы вопроса.\n"
                "- Обоснуй рекомендации: почему они подходят?\n"
                "- Пиши рекомендации развернуто и при необходимости дополняй деталями.\n"
                "Формат ответа:\n"
                "- развернутая рекомендация по проблеме (идея: 'номер идеи', 'статус')\n\n"
                "3️⃣ Ответ основан на:\n"
                "(список) - номер идеи"
            ),
        },
    ]

    response = openai.ChatCompletion.create(
        model="gpt5",
        messages=messages,
    )

    final_response = response["choices"][0]["message"]["content"]
    token_count = response.get("usage", {}).get("total_tokens", 0)
    return final_response.replace("\n", "\n\n").strip(), token_count


def main(user_query):
    """Простой запуск из консоли для проверки: делает поиск и формирует ответ."""
    print("\n[demo] Выполняю поиск через search_problem...")
    metadata_json = search_problem(user_query)
    metadata_list = json.loads(metadata_json).get("results", [])

    final_response, response_tokens = generate_final_response(metadata_list, user_query)
    total_tokens = response_tokens
    print(f"\n[demo] Использовано токенов: {total_tokens}\n")
    return final_response


if __name__ == "__main__":
    user_input = input("Введите запрос: ")
    response = main(user_input)
    print("\nОтвет ассистента:\n")
    print(response)
